spring:
  profiles:
    active: dev
    include:
      - base-addi
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
  output:
    ansi-enabled: always
logging:
  level:
    com.yejin.exam.wbook: debug

#custom:
#  genFileDirPath: c:/temp/app10